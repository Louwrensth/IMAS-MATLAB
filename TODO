- Print context info on error
- Decide on returning 0 (success) or status of ual_end_action
- Decide on action when single field fails (no data): before: assign default value, now: error
- Decide on how to assign error codes for local (mexinterface) functions
- Minimize use of memcpy in get methods (check that no error appears when mex are cleared)
- Enable/Disable memcache from MATLAB
- Decide on STR_0D and STR_1D representation
- Decide on how to allocate arrays of structures

- Call ual_end_action before throwing MEX error and before soft errors (return)
- Remove uncritical error messages for get methods
- Move cast to write_data_from_mxArray
