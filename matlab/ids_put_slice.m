% ids_put_slice(expIdx, IDSpath[, occurence], IDS)
% Stores the IDS slice in the open database
%
% expIdx : index to the database, returned by imas_open/imas_create
% IDSpath : name/occurence of the IDS
% occurence:
% IDS    : the IDS to store in the new slice.
